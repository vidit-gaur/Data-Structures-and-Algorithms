#include<bits/stdc++.h>
using namespace std;
//we will replace all 0s with -1 and make this largestSubarray with sum=0 problem.
int largestSubarray(bool arr[],int n){


}


int main(){
    bool arr[]={0,0,1,1,1,1,1,0};
    cout<<largestSubarray(arr,8);
}